# Project instructions

## Code Style
- Use blank lines to separate logical blocks within functions (e.g., between validation/guard clauses and the return statement, between setup and action, etc.)
- Separate consecutive if blocks with blank lines
- Prefer spelled-out variable names over abbreviations (e.g., `time_limit` not `tl`, `turn_limit` not `nl`, `dataset` not `ds`)
